# Prewired and Environment

This reference summarizes the prewired environment setup for the extracted auth workspace.

## Checklist
- Confirm environment configuration is loaded before runtime.
- Avoid shipping secrets in client-accessible files.
- Document all required launch flags and tokens.
