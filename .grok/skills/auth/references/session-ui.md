# Session UI

This reference documents the session-oriented UI behavior used in the extracted workspace.

## Observations
- Keep session state visible to the user.
- Surface transitions clearly between signed-in and guest flows.
- Maintain accessibility labels for interactive states.
