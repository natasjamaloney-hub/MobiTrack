# Data and Auth

This workspace reference captures the data model and authentication expectations that were bundled in the archive.

## Summary
- Keep user identity and session data isolated per account.
- Prefer server-side validation for sensitive operations.
- Centralize auth state to reduce drift between UI and API flows.
